/** Shared appointment constants — safe to import from client or server. */

export const SLOT_MINUTES = 30;

/** How many days ahead a patient may request an appointment. */
export const BOOKING_WINDOW_DAYS = 45;

/** Minimum lead time (minutes) before the first bookable slot today. */
export const MIN_LEAD_MINUTES = 90;
